<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lqt.baitapbuoi1">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaiTapBuoi1">
        <activity android:name=".adapter.KetNoiDataBase"></activity>
        <activity android:name=".ActivityCauThu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DemoCamera" />
        <activity android:name=".SimpleListView" />
        <activity android:name=".DetailContact" />
        <activity android:name=".CustomListView" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>